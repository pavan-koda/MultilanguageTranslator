# Recommended runtime: Python 3.12 on Windows.
# The current Argos Translate / SentencePiece stack is not compatible with Python 3.13+.
# setuptools 82+ dropped the bundled pkg_resources module, which ctranslate2
# still imports at startup -> ModuleNotFoundError. Pin below that.
setuptools<82
Flask==3.0.0
# argostranslate is intentionally NOT listed here: it pins sentencepiece==0.1.99,
# which has no py3.12 Windows wheel and conflicts with the sentencepiece pin
# below. install.ps1 / install.sh install it separately with --no-deps first:
#   pip install --no-deps argostranslate==1.9.1
sentencepiece>=0.2.1
# 3.20.0's compiled .so requests an executable stack, which fails to load
# under Docker/WSL2 with "cannot enable executable stack": use 4.6.1+ instead.
ctranslate2==4.6.1
stanza==1.1.1
sacremoses==0.0.53
packaging
gunicorn

# torch 2.12+ wheels crash (DLL init failure) on this Broadwell Xeon; 2.4.1 works.
torch==2.4.1

# IndicTrans2 remote code needs transformers 4.x (uses transformers.onnx + torch_dtype).
transformers==4.44.2
sacrebleu
# Current IndicTransToolkit release needs MSVC to build its Cython extension;
# this commit is the last pure-Python version (install with --no-build-isolation
# --no-deps after installing poetry-core and the indic_nlp_library below):
# pip install git+https://github.com/VarunGumma/indic_nlp_library
# pip install --no-build-isolation --no-deps git+https://github.com/VarunGumma/IndicTransToolkit@0c607654e8

# llama-cpp-python is intentionally NOT listed here: it needs a different
# wheel per machine (CUDA build on a GPU Linux box, CPU-only on Windows —
# see scripts/install_llama_cpp.py for why). Run that script (or install.ps1
# / install.sh, which call it) to install the right build for this machine.

huggingface_hub
